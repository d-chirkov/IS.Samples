@model string
@{
    ViewBag.Title = "Сайт 1";
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>
    <div>
        <h1>Сайт 1 - Главная @(Model == null ? "" : "(" + Model + ")")</h1>
        @Html.ActionLink("В профиль", "Account")
        @Html.ActionLink("Выход", "Logout")
    </div>
</body>
</html>

